name: prober2

on: [push]

jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: echo
        working-directory: contracts
        run: echo "checkout + workdir ok"
